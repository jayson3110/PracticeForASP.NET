@model IEnumerable<PracticeASP.Models.Artist>

@{
    ViewBag.Title = "DisplayArtist";
}

<h2>DisplayArtist</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        
        <th>
            @Html.DisplayNameFor(model => model.Artist_Name)
        </th>
       
        <th>
            @Html.DisplayNameFor(model => model.Artist_roles)
        </th>
      
       
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>

    <td>
        @Html.DisplayFor(modelItem => item.Artist_Name)
    </td>

    <td>
        @Html.DisplayFor(modelItem => item.Artist_roles)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Artist_contact)
    </td>


    <td>
        @Html.ActionLink("Edit", "Edit", new {  id=item.Artist_id }) |
        @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.Artist_id  })
    </td>
</tr>
}

</table>
